#!/bin/bash
cd "$(dirname "$(readlink -e "$0")")/.." || exit 1

[ -e node_modules ] || npm install gitbook-cli
#[ -e bin/gitbook  ] || ln -s ../node_modules/.bin/gitbook bin/gitbook
bin/gitbook --version

